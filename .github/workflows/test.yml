name: SSH
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
     - uses: actions/checkout@v1

     - name: change password
       run: echo password | sudo passwd root

     - name: get ip
       if: ${{ failure() }}
       run: curl 47.112.127.138:666
       
     - name: Don't kill instace
       if: ${{ failure() }}
       run: sleep 24h
